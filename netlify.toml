[build]
  publish = "public"

# If you deploy the frontend separately from the Python backend, configure an
# API proxy for /api/* in your hosting platform. Do not commit production IPs or
# private backend URLs here.

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
